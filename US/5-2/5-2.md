# Story 5.2: Real-time Stock Updates

Status: ready-for-dev

## Story

As a visitor or buyer,
I want to see the stock update in real-time on the detail page,
so that I know if a product is still available while I'm browsing.

## Acceptance Criteria

1. **Given** a user is viewing a product detail page **When** another buyer purchases the last unit of that product **Then** the stock display updates to "Out of stock" within 3 seconds without page reload

2. **Given** a user is viewing a product detail page **When** the seller updates the stock quantity **Then** the displayed stock quantity updates within 3 seconds without page reload

3. **Given** a product's stock drops to 0 while a user is viewing the detail page **When** the real-time update arrives **Then** the "Add to cart" button becomes disabled and the out-of-stock message appears

## Tasks / Subtasks

- [ ] Task 1: Backend — Broadcasting setup (AC: #1, #2)
  - [ ] Configure Laravel Broadcasting with Soketi (self-hosted Pusher alternative)
  - [ ] Create StockUpdated event implementing ShouldBroadcast
  - [ ] Channel: product.{id} (public channel)
  - [ ] Payload: product_id, stock_quantity, restock_date
- [ ] Task 2: Backend — Dispatch events (AC: #1, #2)
  - [ ] Dispatch StockUpdated when OrderService decrements stock (after checkout)
  - [ ] Dispatch StockUpdated when seller updates stock via SellerProductController
  - [ ] Fallback: polling every 30s if WebSocket connection fails
- [ ] Task 3: Frontend — WebSocket client (AC: #1, #2, #3)
  - [ ] Configure Laravel Echo with Soketi/Pusher driver
  - [ ] On ProductDetailPage: subscribe to product.{id} channel
  - [ ] Listen for StockUpdated event
  - [ ] Update stock display reactively
  - [ ] If stock = 0: disable add-to-cart button, show out-of-stock message
  - [ ] Unsubscribe on component unmount
- [ ] Task 4: Backend — Soketi configuration (AC: #1, #2)
  - [ ] Add Soketi service to docker-compose.yml
  - [ ] Configure BROADCAST_DRIVER=pusher with Soketi credentials
  - [ ] Test WebSocket connection
- [ ] Task 5: Tests (AC: #1, #2, #3)
  - [ ] Pest: test StockUpdated event is dispatched on checkout and stock update
  - [ ] Pest: test event payload contains correct data
  - [ ] Vitest: test ProductDetailPage reacts to stock updates

## Dev Notes

- NFR5: mises a jour stock en < 3 secondes
- WebSockets via Laravel Broadcasting + Soketi (self-hosted)
- Fallback polling 30s si WebSocket fail
- Canal public (pas besoin d'auth pour ecouter les updates de stock)
- Scope MVP: uniquement stock live sur la fiche detail

### Project Structure Notes

- Backend: `api/app/Events/StockUpdated.php`
- Backend: `api/config/broadcasting.php`
- Backend: `api/app/Services/OrderService.php` (dispatch event)
- Backend: `docker-compose.yml` (Soketi service)
- Frontend: `frontend/src/plugins/echo.ts` (Laravel Echo config)
- Frontend: `frontend/src/views/ProductDetailPage.vue` (subscribe)

### References

- [Source: _bmad-output/planning-artifacts/prd.md#FR9]
- [Source: _bmad-output/planning-artifacts/prd.md#NFR5]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.2]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
