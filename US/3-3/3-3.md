# Story 3.3: Product Filters

Status: ready-for-dev

## Story

As a visitor or buyer,
I want to filter products by category, price range and name,
so that I can narrow down results to find exactly what I need.

## Acceptance Criteria

1. **Given** a user is on the catalogue page **When** they apply a category filter **Then** only products matching the selected category are displayed

2. **Given** a user sets a price range filter (min and/or max) **When** the filter is applied **Then** only products within the price range are displayed

3. **Given** a user types a search term in the name filter **When** the filter is applied **Then** only products whose name matches the search term are displayed

4. **Given** a user applies multiple filters simultaneously **When** filters are combined **Then** results match ALL active filters **And** results return in under 1 second

## Tasks / Subtasks

- [ ] Task 1: Backend — Filter query parameters (AC: #1, #2, #3, #4)
  - [ ] Add query params to product list endpoint: category, price_min, price_max, search
  - [ ] Build Eloquent query with conditional where clauses
  - [ ] Search by name with LIKE %term%
  - [ ] Combine all filters (AND logic)
  - [ ] Ensure index on price and category columns for performance
- [ ] Task 2: Frontend — Filter panel (AC: #1, #2, #3, #4)
  - [ ] Create ProductFilters.vue component
  - [ ] Category dropdown/chips (8 categories)
  - [ ] Price range inputs (min, max)
  - [ ] Search text input with debounce (300ms)
  - [ ] Apply filters on change, update URL query params
  - [ ] Reset filters button
- [ ] Task 3: Frontend — Filter integration (AC: #4)
  - [ ] Connect filters to catalog store
  - [ ] Fetch filtered products from API
  - [ ] Update ProductGrid with results
  - [ ] Show "No results" message when filters match nothing
- [ ] Task 4: Tests (AC: #1, #2, #3, #4)
  - [ ] Pest: test each filter individually, combined filters, empty results
  - [ ] Vitest: test ProductFilters component

## Dev Notes

- NFR4: filtres retournent resultats en < 1 seconde
- Differenciateur cle: trouver le bon composant en < 3 clics
- Debounce cote frontend pour eviter les requetes excessives
- Filtres dans les query params URL (partageables, bookmarkables)
- Index DB sur price et category pour la performance

### Project Structure Notes

- Backend: `api/app/Http/Controllers/Api/CatalogController.php` (ajouter filtres)
- Frontend: `frontend/src/components/ProductFilters.vue`
- Frontend: `frontend/src/stores/catalog.ts` (ajouter actions filtrage)

### References

- [Source: _bmad-output/planning-artifacts/prd.md#FR7]
- [Source: _bmad-output/planning-artifacts/prd.md#NFR4]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 3.3]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
