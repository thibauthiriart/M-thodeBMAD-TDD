# Story 4.3: Cart Management

Status: ready-for-dev

## Story

As a buyer,
I want to view my cart with a summary of products, quantities and total price,
so that I can review my order before checkout.

## Acceptance Criteria

1. **Given** a logged-in buyer navigates to their cart **When** the cart page loads **Then** they see all products with photo, name, unit price, quantity, line total, and cart total

2. **Given** a buyer changes the quantity of a product in the cart **When** the new quantity is saved **Then** the line total and cart total are recalculated and updated

3. **Given** a buyer sets the quantity to 0 or clicks "Remove" **When** the action is confirmed **Then** the product is removed from the cart and totals are updated

4. **Given** a buyer has an empty cart **When** they view the cart page **Then** they see an empty state with a link to browse the catalogue

## Tasks / Subtasks

- [ ] Task 1: Backend — Cart endpoints (AC: #1, #2, #3)
  - [ ] Create CartController@show (GET /api/cart) — return full cart with items, products, totals
  - [ ] Create CartController@updateItem (PUT /api/cart/items/{id}) — update quantity
  - [ ] Create CartController@removeItem (DELETE /api/cart/items/{id}) — remove item
  - [ ] Create CartResource with nested CartItemResource
  - [ ] Calculate line totals and cart total
  - [ ] Auth + role:acheteur middleware
- [ ] Task 2: Frontend — Cart page (AC: #1, #2, #3, #4)
  - [ ] Create CartPage.vue
  - [ ] Add route /cart (protected, role: acheteur)
  - [ ] Display items: photo, name, unit price, quantity input, line total
  - [ ] Display cart total at bottom
  - [ ] Quantity change → API call → recalculate totals
  - [ ] Remove button → API call → remove item
  - [ ] Empty state with link to /categories
- [ ] Task 3: Frontend — Cart store updates (AC: #1, #2, #3)
  - [ ] Add fetchCart, updateItemQuantity, removeItem actions to useCartStore
  - [ ] Computed: cartTotal, itemCount
- [ ] Task 4: Tests (AC: #1, #2, #3, #4)
  - [ ] Pest: test cart show, update quantity, remove item, empty cart
  - [ ] Vitest: test CartPage component

## Dev Notes

- NFR12: acheteur accede uniquement a SON panier
- NFR18: donnees du panier persistees en DB
- Recalcul des totaux cote serveur (source de verite)
- Frontend affiche le resultat du serveur apres chaque action
- Pas de validation de stock ici (sera faite au checkout)

### Project Structure Notes

- Backend: `api/app/Http/Controllers/Api/CartController.php`
- Backend: `api/app/Http/Resources/CartResource.php`
- Backend: `api/app/Http/Resources/CartItemResource.php`
- Frontend: `frontend/src/views/CartPage.vue`
- Frontend: `frontend/src/stores/cart.ts`

### References

- [Source: _bmad-output/planning-artifacts/prd.md#FR14]
- [Source: _bmad-output/planning-artifacts/prd.md#FR15]
- [Source: _bmad-output/planning-artifacts/prd.md#NFR12]
- [Source: _bmad-output/planning-artifacts/prd.md#NFR18]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 4.3]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
