{
  "name": "testapp-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for testApp (Playwright + DB assertions)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^25.2.3",
    "dotenv": "^16.4.0",
    "mysql2": "^3.12.0",
    "typescript": "^5.9.3"
  }
}
